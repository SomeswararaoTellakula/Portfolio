const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const canvas=$('#particle-canvas'),ctx=canvas.getContext('2d'),points=[];let dpr=Math.min(devicePixelRatio||1,2),mouse={x:-999,y:-999};
function resize(){canvas.width=innerWidth*dpr;canvas.height=innerHeight*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}function seed(){points.length=0;const n=innerWidth<768?350:700;for(let i=0;i<n;i++)points.push({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22,r:Math.random()*1.5+.4})}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const p of points){const dx=p.x-mouse.x,dy=p.y-mouse.y,d=Math.hypot(dx,dy);if(d<160){p.x+=dx/d*(160-d)*.001;p.y+=dy/d*(160-d)*.001}p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;ctx.fillStyle=getComputedStyle(document.body).getPropertyValue('--signal');ctx.globalAlpha=.25;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}ctx.strokeStyle=getComputedStyle(document.body).getPropertyValue('--slate');for(let i=0;i<points.length;i+=2)for(let j=i+1;j<Math.min(i+12,points.length);j++){const a=points[i],b=points[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<92){ctx.globalAlpha=(1-d/92)*.16;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}requestAnimationFrame(draw)}resize();seed();draw();addEventListener('resize',()=>{resize();seed()});addEventListener('pointermove',e=>{mouse={x:e.clientX,y:e.clientY};$('.spotlight').style.left=e.clientX+'px';$('.spotlight').style.top=e.clientY+'px'});addEventListener('scroll',()=>$('#progress-bar').style.width=(scrollY/(document.body.scrollHeight-innerHeight)*100)+'%');
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');e.target.querySelectorAll('[data-count]').forEach(countUp)}}),{threshold:.15});$$('.reveal').forEach(e=>observer.observe(e));function countUp(el){if(el.dataset.done)return;el.dataset.done=1;const end=+el.dataset.count,start=performance.now();function tick(now){const p=Math.min((now-start)/700,1);el.textContent=(el.dataset.prefix||'')+Math.round(end*p);if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)}
$('#theme-toggle').onclick=()=>document.body.classList.toggle('dark');$('#effects-toggle').onclick=e=>{document.body.classList.toggle('calm');e.target.textContent=document.body.classList.contains('calm')?'Effects: calm':'Effects: full'};$('#menu-button').onclick=()=>document.querySelector('nav').classList.toggle('open');$$('nav a').forEach(a=>a.onclick=()=>document.querySelector('nav').classList.remove('open'));
const knee=$('#knee'),angle=$('#angle'),form=$('#form');let kneeX=120;function setKnee(x){kneeX=Math.max(92,Math.min(180,x));knee.setAttribute('cx',kneeX);const value=Math.round(118+(kneeX-120)*1.5);angle.textContent=value+'°';form.textContent=value<90?'Good form':'Go lower'}knee.onkeydown=e=>{if(e.key==='ArrowLeft'||e.key==='ArrowRight'){e.preventDefault();setKnee(kneeX+(e.key==='ArrowRight'?5:-5))}};let dragging=false;knee.onpointerdown=()=>dragging=true;addEventListener('pointerup',()=>dragging=false);addEventListener('pointermove',e=>{if(dragging){const r=knee.closest('svg').getBoundingClientRect();setKnee((e.clientX-r.left)/r.width*300)}});
const grid=$('#density-grid'),cells=[];for(let i=0;i<96;i++){const b=document.createElement('button');b.type='button';b.setAttribute('aria-label','Add person');b.dataset.count=0;b.onclick=()=>{b.dataset.count=+b.dataset.count+1;const total=cells.reduce((n,c)=>n+(+c.dataset.count),0),r=$('#risk');r.textContent=total>12?'High risk':total>4?'Moderate risk':'Low risk';r.style.color=total>12?'var(--hot)':total>4?'var(--warm)':'var(--signal)'};grid.appendChild(b);cells.push(b)}$('#failure').onclick=e=>{e.disabled=true;let n=5;e.textContent=`Reconnecting ${n}s`;const id=setInterval(()=>{n--;e.textContent=n?`Reconnecting ${n}s`:'Camera restored';if(!n){clearInterval(id);e.disabled=false}},1000)};
const modules=['reachability','structured data','accessibility','freshness','engagement','cross-page checks'];$('#run-audit').onclick=()=>{const wrap=$('#audit-modules'),out=$('#audit-output');wrap.innerHTML='';out.textContent='{ "status": "running" }';modules.forEach((name,i)=>setTimeout(()=>{const s=document.createElement('span');s.textContent='✓ '+name;wrap.appendChild(s);if(i===modules.length-1)out.textContent=JSON.stringify({url:$('#audit-url').value,status:'complete',modules:6,checks:7,sample:20},null,2)},i*260))};
const skills=$$('.skills button'),nodes=$$('.project-nodes button');function highlight(el){const ids=el.dataset.projects?.split(',')||[el.dataset.node];nodes.forEach(n=>n.classList.toggle('active',ids.includes(n.dataset.node)))}skills.forEach(s=>{s.onmouseenter=()=>highlight(s);s.onclick=()=>highlight(s)});nodes.forEach(n=>n.onmouseenter=()=>highlight(n));$('#copy-email').onclick=async e=>{try{await navigator.clipboard.writeText('someshtellakula@gmail.com')}catch{}e.target.textContent='Copied';setTimeout(()=>e.target.textContent='Copy email',2000)};
const data={name:'Someswara Rao Tellakula',role:'backend and full-stack developer',education:['BE Information Technology, Vasavi College of Engineering — expected 2027 — CGPA 7.22/10','BS in Data Science, IIT Madras — 2024 to present'],projects:['DeepFit AI','CrowdGuard','SiteLens'],contact:{email:'someshtellakula@gmail.com',github:'https://github.com/SomeswararaoTellakula',linkedin:'TODO_LINKEDIN_URL'}};$('#json-output').textContent=JSON.stringify(data,null,2);$('#json-toggle').onclick=()=>{$('#human-view').hidden=true;$('#json-view').hidden=false;scrollTo(0,0)};$('#json-back').onclick=()=>{$('#human-view').hidden=false;$('#json-view').hidden=true;scrollTo(0,0)};
